name: Build with Maven
description: "Build project using Maven"
runs:
  using: "composite"
  steps:
    - name: Build with Maven
      run: |
        echo "Building the project using Maven..."
        mvn clean install
